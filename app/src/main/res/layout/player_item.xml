<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/list_style"
    android:padding="14dp">

    <ImageView
        android:id="@+id/pic_listview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:scaleType="centerCrop"
        android:src="@drawable/ic_contact_picture"/>

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_toRightOf="@id/pic_listview"
        android:layout_alignBottom="@id/pic_listview"
        android:layout_alignTop="@id/pic_listview"
        android:paddingLeft="10dp"
        android:paddingRight="10dp"
        android:paddingBottom="6dp"
        android:orientation="vertical"
        android:gravity="center">

        <TextView
            android:id="@+id/name_listview"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:maxLines="1"
            android:marqueeRepeatLimit="marquee_forever"
            android:ellipsize="marquee"
            android:scrollHorizontally="true"
            android:textSize="24sp"
            android:text="Player 1"/>

        <TextView
            android:id="@+id/score_listview"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Score: 0"
            android:textSize="14sp"
            android:textColor="@color/default_text"/>
    </LinearLayout>

</RelativeLayout>